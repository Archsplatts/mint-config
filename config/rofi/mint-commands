	--- Mise à jour ---
			
󰚰  sudo apt update && apt upgrade

	--- Paquets ---
			
  apt search <nom>
  sudo apt install <nom>

󱧖  sudo apt remove <nom>
   sudo apt purge <nom>

  dpkg -s <nom> (paquet installé)
   apt show <nom> (paquet dépôts)

	--- Nettoyage ---
			
󰃢  sudo apt autoclean
   sudo apt clean

󱧖  sudo apt autoremove
		
